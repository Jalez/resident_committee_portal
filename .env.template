# ============================================
# SITE CONFIGURATION
# ============================================

# Name of your resident committee (shown in page titles and header)
SITE_NAME=Resident Committee

# Short name for the header (optional, uses SITE_NAME if not set)
# SITE_SHORT_NAME=RC

# Description for SEO meta tags
SITE_DESCRIPTION=Tenant Committee Portal

# ============================================
# DATABASE
# ============================================

# PostgreSQL Connection String
# Local dev: postgres://user:password@localhost:5432/portal
# Neon: postgres://user:password@ep-xxx.region.neon.tech/database?sslmode=require
DATABASE_URL=postgres://postgres:postgres@localhost:5432/portal

# Database Provider (optional)
# - "postgres": Standard PostgreSQL (default in development)
# - "neon": Neon serverless PostgreSQL (default in production)
# If not set, defaults to "postgres" locally and "neon" in production
# DATABASE_PROVIDER=postgres

# ============================================
# GOOGLE APIs
# ============================================

# Google API Key (for reading public data)
# Must have Calendar API and Drive API enabled in Google Cloud Console.
GOOGLE_API_KEY=

# Google Calendar
# The ID of the public calendar to display events from (e.g. your-org@gmail.com).
GOOGLE_CALENDAR_ID=

# Google Drive PUBLIC Root Folder
# The ID of the public root folder in Google Drive that contains the year-based structure.
# Example: https://drive.google.com/drive/folders/1-4ZDizK5Sbr2kR-o95QfsxP6zUsu0vPH
# The ID is the last part: 1-4ZDizK5Sbr2kR-o95QfsxP6zUsu0vPH
# Structure:
# - PublicRoot/
#   - 2026/
#     - minutes/ (Folder)
# Ensure this root folder and its children are shared with "Anyone with the link" as viewer.
GOOGLE_DRIVE_PUBLIC_ROOT_ID=

# ============================================
# SERVICE ACCOUNT (for writing to private sheets)
# ============================================

# Google Service Account Email
# Create a service account in Google Cloud Console, then share the sheet with this email as Editor.
# Format: your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_EMAIL=

# Google Service Account Private Key
# The private key from the service account JSON file.
# IMPORTANT: Replace all newlines with \n (the key should be one long string in .env)
# Example: -----BEGIN PRIVATE KEY-----\nMIIEvQ...rest of key...\n-----END PRIVATE KEY-----\n
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=



# ============================================
# ADMIN AUTHENTICATION (OAuth 2.0)
# ============================================

# Google OAuth 2.0 Client ID
# Create OAuth 2.0 credentials in Google Cloud Console -> APIs & Services -> Credentials
# Application type: Web application
# Authorized redirect URI: http://localhost:5173/auth/callback (for dev)
GOOGLE_OAUTH_CLIENT_ID=

# Google OAuth 2.0 Client Secret
GOOGLE_OAUTH_CLIENT_SECRET=

# Admin Email
# Only this email address can access admin routes (e.g., /admin/board)
ADMIN_EMAIL=

# Session Secret
# A random string used to sign session cookies. Generate with: openssl rand -base64 32
SESSION_SECRET=

# ============================================
# CROSS-ACCOUNT PROTECTION (RISC)
# ============================================

# Google OAuth 2.0 Client IDs (comma-separated if multiple)
# These are used to validate the audience (aud) claim in Security Event Tokens.
# If not set, it defaults to the GOOGLE_OAUTH_CLIENT_ID used for admin auth.
GOOGLE_CLIENT_IDS=

# RISC Receiver Endpoint URL
# The public HTTPS URL where Google will post security event tokens.
# Example: https://your-domain.com/api/risc/receiver
RISC_RECEIVER_URL=

# ============================================
# EMAIL (Resend)
# ============================================

# Resend API Key
# Create at https://resend.com/api-keys
# IMPORTANT: For inbound email reply handling, use a key with "Full Access"
# (not just "Sending access") so it can fetch email content via the Receiving API.
RESEND_API_KEY=

# Sender Email
# The email address used to send reimbursement request notifications.
# Must be verified in Resend, or use the default "onboarding@resend.dev" for testing.
SENDER_EMAIL=onboarding@resend.dev

# Recipient Email
# The email address that receives reimbursement request notifications
# (e.g., the building owner or treasurer).
RECIPIENT_EMAIL=

# ============================================
# INBOUND EMAIL REPLY HANDLING (Optional)
# ============================================
# These are only needed if you want to enable automatic status updates
# based on email replies from the recipient.

# Resend Inbound Email Address
# Your Resend receiving address for inbound email (find it in Resend dashboard → Emails → Receiving).
# Format: anything@your-resend-id.resend.app
# RESEND_INBOUND_EMAIL=

# Resend Webhook Signing Secret
# Create a webhook in Resend dashboard pointing to: https://your-domain.com/api/webhooks/resend
# Select event: email.received
# Copy the signing secret (starts with "whsec_")
# RESEND_WEBHOOK_SECRET=
